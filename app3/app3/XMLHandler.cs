using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace app3
{
    public class XMLHandler
    {
        /*
            using System.Xml; 
            using System.Text; 

            … 

            XmlTextWriter wr = new XmlTextWriter("Geo.xml", Encoding.UTF8);
            wr.Formatting = Formatting.Indented;
            wr.WriteStartDocument(true);
            wr.WriteStartElement("géo");
            wr.WriteStartElement("pays");  wr.WriteAttributeString("État", "République");
            wr.WriteElementString("nom", "France");
            wr.WriteElementString("capitale", "Paris");
            wr.WriteElementString("habitants", "60");
            wr.WriteEndElement();
            wr.WriteEndDocument();
            wr.Flush();
            wr.Close();
        */

        public void createEtudiantsXML(List<Etudiants> list)
        {

        }

        public void createCoursXML(List<Cours> list)
        {

        }

        public void createResultatsXML(List<Resultats> list)
        {

        }
    }
}
